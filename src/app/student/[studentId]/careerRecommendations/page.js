import {Box} from '@mui/material';
import CareerRecomendation from '@/components/careerRecommendation';

const CareerRecommendationsPage = () => {
    return (
        <Box className="main">
            <CareerRecomendation />
        </Box>
    );
};

export default CareerRecommendationsPage;
